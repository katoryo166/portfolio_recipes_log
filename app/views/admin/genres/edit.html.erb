<div class="row">
  <div class="col-md-12 col-xs-12 offset-md-1">
    <h2>ジャンル編集</h2>
    <div class="form-group">
      <%= form_with model:@genre, url:admin_genre_path, local:true do |f| %>
      <%= render "layouts/error_messages", model: @genre %>
        <%= f.label:"ジャンル名" %>
        <%= f.text_field :name, class:"col-md-4" %>
        <%= f.submit "変更を保存する", class:"btn-sm btn-success"%>
      <% end %>
      </div>

      <div class="form-group">
        <%= link_to "削除する", admin_genre_path(@genre), class:"btn btn-danger", method: :delete, "data-confirm"=>"削除しますか？" %>
      </div>
  </div>
</div>